import { DonutChartDirective } from './donut-chart.directive';

xdescribe('DonutChartDirective', () => {
  it('should create an instance', () => {
    //const directive = new DonutChartDirective();
    //expect(directive).toBeTruthy();
  });
});
