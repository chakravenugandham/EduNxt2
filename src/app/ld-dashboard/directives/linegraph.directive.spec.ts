import { LinegraphDirective } from './linegraph.directive';

describe('LinegraphDirective', () => {
  it('should create an instance', () => {
    const directive = new LinegraphDirective();
    expect(directive).toBeTruthy();
  });
});
